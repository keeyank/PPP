// Program breaks with 0 inputs - outputs that the sum is 0, temp
// is 0 for both min and max - this isn't the case
// Also, for 1 input, the program breaks - that input should be max and min, not just 1